<UserControl x:Class="View.EventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:View"
             xmlns:viewModel="clr-namespace:ViewModel.MainWindow.Sections;assembly=ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="519" d:DesignWidth="824"
             Background="Transparent">
    
    <UserControl.DataContext>
        <viewModel:EventsViewModel/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Border Grid.Row="1">
            <DataGrid x:Name="eventsGrid" AutoGenerateColumns="True" ItemsSource="{Binding EventsList}">
                
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
